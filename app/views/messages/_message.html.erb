<div class="message mb-2 <%= "me" if message.account.id == current_account.id %>">
  <div class="content-container">


  <div class="content">

  <%= message.content %>
  </div>
  <div class="author">
    <%= link_to message.account.username, profile_path(message.account.username), class: "no-underline"   %>
  </div>
    </div>
</div>
